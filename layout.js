const FIXED={america:[300,110],arlinda:[900,110],zeli:[300,300],terezinha:[900,300],elielton:[430,500],elenilton:[700,500],sheila:[825,500],ritheli:[1080,500],joyce:[755,715]};
export function buildLayout(people,relations){const out={},known=new Set(people.map(p=>p.id));for(const [id,p] of Object.entries(FIXED))if(known.has(id))out[id]=[...p];const uncertain=people.filter(p=>p.evidenceStatus==='investigate'&&!out[p.id]);uncertain.forEach((p,i)=>out[p.id]=[260+i*245,-70]);const rest=people.filter(p=>!out[p.id]);rest.forEach((p,i)=>out[p.id]=[280+(i%4)*220,900+Math.floor(i/4)*170]);return out;}
